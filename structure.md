The app has a daily streak system
The app feature many courses which inlcude but not limited to : 
 - frontend with next
 - frontent with react
 - backend with python
 - backend with node
 - backend with fastapi
 - backend with express
 - data base
 You will start with only one course "Frontend with react" but you will leave place for adding others
 The app will use firebase for authentication and database and access to AI. You will use the cheapest model in firebase ( gemini flash lite)
 

The app will use best practices in nextjs development.

Start by analyzing

Each course will have many chapters. O
Each chapter will contain subchapters.
Each subchatper will contain a 5 minute size lesson that contains quizz, exercise and a small quiz at the end and also write code exercices. 
The user will be able to ask the ai for exaplanation. 
Each lesson will contain many part that contains code and explantions until the user learn the topic. 

Each course will be a complete roadmap that not only teach the topic but also provide projects and a job roadmap and all the things the user need to know to be good at the topic.